using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SliderMovimientoEjeX : MonoBehaviour
{
    public GameObject rama;
    public Slider slider;
    public float movementSpeed;

    /*private void Start()
    {
        slider.onValueChanged.AddListener(delegate { onValueXChanged(); });
    }

    public void onValueXChanged()
    {
        int sliderValue = ((int)slider.value);
        if (sliderValue < 0)
        {
            aumentarEjeX(sliderValue);
        }
        else
        {
            disminuirEjeX(sliderValue);
        }
    }*/

}
